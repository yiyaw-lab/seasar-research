# Human-judge validation sheet — detection critiques

**Task.** Two LLM judges (Opus + Sonnet) scored every enumeration below as *surfacing* its seam's target requirement (agreement ~99%). Independently decide, for each item, whether the enumeration **genuinely names the target requirement in substance** (not just generic 'add tests'/'handle errors'). Mark **Y** or **N** in the `HUMAN` box. Then compute your agreement with the LLM verdict (LLM = Y for all items here).

Agreement = (# you marked Y) / (total). If high, the LLM judge is validated (not lenient).


## PAGINATION — TARGET: repo.list must return ALL records in the group in ONE call (no default pageSize cap / no pagination by default) — else the report, which counts a single list() call, silently UNDERCOUNTS.

### Item 1 (pagination)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- DEFAULT MUST NOT TRUNCATE: when opts.pageSize is omitted, list(group) must return EVERY record in the group, not a default-sized page. The whole system is a counter — the reporting service calls list(group) ONCE and counts the result (probes/harness.ts: `createRepo(store).list('g').length`). The SPEC's phrase 'a sensible default applies when it is omitted' is the trap: if 'sensible default' is int
- GROUP ISOLATION IS UNCONDITIONAL: the filter `item.group === group` must be applied on EVERY path, including the paging path, and must run BEFORE slicing. If a future edit slices store.all() first and filters second (or pages the unfiltered list), a page could contain records from 'other' groups, violating the spec's hard constraint 'do not return records from other groups'. Verified: list('g') an
- EXACT-MATCH GROUP SEMANTICS: group membership must be strict equality on the full group string, not a prefix/substring/case-insensitive match. types.ts gives group as an opaque string; the harness uses groups 'g' and 'other'. A loose match (e.g. startsWith) would fold distinct groups together and overcount. Verified: an unknown group returns 0, and 'g' does not pull in 'other'.
- PAGING, IF ANY, PRESERVES STORE ORDER AND IS A STABLE FIRST-PAGE: when pageSize IS supplied the returned slice must be a contiguous prefix of the group's records in store.all() order (no reorder, no random sampling, no dedupe), so that paged reads compose into the same total as one unpaged read. Otherwise a caller that DOES page (the spec explicitly contemplates 'callers that page') cannot reassem
- NON-DESTRUCTIVE / NO ALIASING OF STORE STATE: list must not mutate the array returned by store.all() nor return an alias the caller can mutate to corrupt the store. Array.filter already returns a fresh array (verified the result is not the same array as store.all() and a second call still returns 150), but any future in-place sort/splice on store.all() before slicing would silently mutate shared s
- MALFORMED pageSize MUST DEGRADE TO 'COUNT ALL', NOT TO ZERO/NaN: pageSize is typed number? but is unvalidated input; values like negative, NaN, fractional, or Infinity must not silently zero out or distort the count. The implementation only truncates on an integer >= 0 and otherwise falls through to returning the full group, so a NaN/negative pageSize counts all (matching the omitted-default contr

### Item 2 (pagination)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- The default (opts omitted) MUST return the COMPLETE set of in-group records, not a single default-sized page. The spec says 'a sensible default applies when pageSize is omitted,' but the sole consumer is a counter that does `repo.list(group).length` in ONE call (harness: expects 150 of 150). If 'sensible default' is implemented as a finite default page size (e.g. slice(0, 50/100/20)), the code com
- Group filtering MUST be exact-match isolation: the result contains records whose `group` equals the requested group and NO records from any other group. The harness mixes 150 'g' + 10 'other' and asserts the count is exactly 150. A bug that uses substring/prefix matching, `startsWith`, case-insensitive compare, `==` coercion, or filters on the wrong field (id vs group) compiles fine but leaks or d
- If pagination is real (pageSize given), paging MUST be over the IN-GROUP subset and MUST be complete and non-overlapping across pages so that a caller summing page lengths recovers the true group total. The order of operations is load-bearing: filter-by-group THEN page. If the implementation pages the raw store first and filters second (slice the global list, then drop out-of-group), each page is 
- `list` MUST be a pure read with no observable side effects on the store and no dependence on call history — repeated calls with the same group return the same complete set (idempotent). The Store contract is just `all(): Item[]`; the harness calls `all()` returning `items.slice()` (a defensive copy). The repo MUST NOT mutate the array returned by `all()` (e.g. in-place `sort`, `splice` for paging)
- The pageSize default and validation must handle the degenerate/boundary inputs the contract leaves open WITHOUT silently zeroing the result: pageSize of 0, negative, NaN, non-integer, or larger-than-the-group. The reference uses `opts?.pageSize !== undefined`, so pageSize:0 takes the slice path and `slice(0,0)` returns [] — a count of 0. The system must define whether 0/invalid means 'no items', '
- When pageSize is provided, the contract MUST be consistent about whether a single `list` call returns ONLY the first page or the whole group, AND if it returns only the first page there MUST be a way to request subsequent pages (an offset/cursor) — otherwise pagination is unobservable beyond page 1 and any total computed from `list` is capped at one page. types.ts ListOpts has ONLY `pageSize` (no 
- The repo MUST tolerate the actual data shape the store yields: groups with more records than any default page (150 > typical defaults), an empty store, and duplicate or repeated ids within a group, counting each physical record once. The counter's correctness depends on result length equaling the number of in-group records; any implicit de-duplication by id, or a default cap below the largest real

### Item 3 (pagination)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- The omitted-opts default must NOT cap the result set. The whole reason this system breaks silently: the reporter calls list('g') once with no opts and counts .length, expecting every record. If 'a sensible default pageSize' is read as 'apply a default page limit (e.g. 50/100/1000) even when the caller did not ask to page', the single call returns one page and the reporter under-counts (150 -> 50) 
- Group isolation must be exact-match and total — list(group) returns ONLY items whose group strictly equals the argument, and ALL of them. This is the one constraint the SPEC states explicitly ('do not return records from other groups'), but its dual (must return all matching) is unstated and equally load-bearing for a counter. A prefix/substring/case-insensitive/loose comparison, or filtering the 
- list must read a fresh snapshot from store.all() on every call and must not assume store.all() returns a stable/shared/cached array. The given Store.all() returns items.slice() — a NEW array each call. An implementation that caches the first result, or that mutates/sorts the array in place (corrupting the store's source for the reporter or for a second caller), or that holds a reference expecting 
- No deduplication or set-collapsing of records by id. The counter counts records, not distinct ids. Item.id is not guaranteed unique within a group (nothing in the types enforces it), and a 'helpful' implementation that de-dupes by id, or builds a Map keyed by id, would collapse legitimate duplicate-id records and under-count.
- When opts IS supplied for paging, list must return a CONTIGUOUS, NON-OVERLAPPING, COMPLETE partition across pages so a paging caller that sums page lengths gets the same total as a non-paging caller. The SPEC offers no offset/cursor param, so for paging to be usable at all the page boundaries and ordering must be deterministic and exhaustive; an unstable filter order or a slice that drops/duplicat
- A non-meaningful pageSize (0, negative, NaN, non-integer) must not be silently interpreted as a real page cap that zeroes or corrupts the result. ListOpts.pageSize is typed as number, which admits 0/-5/NaN/2.5; a naive slice(0, pageSize) yields [] for 0/negative and unpredictable behavior for NaN, turning a caller bug into a silent empty count rather than a full result or an error.

### Item 4 (pagination)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- No-opts default must NOT truncate. When list(group) is called with opts entirely omitted, every matching record must be returned. This is the central trap of THIS system: the spec says 'a sensible default applies when pageSize is omitted', and the sole consumer (the reporting service) counts list(group).length on a single un-paged call. A default page size may only take effect once a caller has op
- Group isolation must be exact-match and total. list(group) returns ONLY records whose item.group === group, never records from any other group, and never records merely containing/startsWith/related to the group string. This constraint is purely a runtime predicate; TypeScript cannot enforce it because every Item is structurally identical regardless of its group value.
- list() must re-read the store on every call and must not depend on identity/order/reference of Store.all()'s result. all() returns a fresh slice() each call, so the repo cannot cache the first result, hold a stale reference, or assume a stable array across calls; each list() must reflect the current store contents.
- list() must not mutate the array Store.all() returns nor any Item in it (no in-place sort/splice/reverse, no object mutation). The store hands back a slice but the elements are shared references; the filter+slice pipeline must be non-destructive.
- Paging must slice the GROUP-FILTERED set, not the raw store. When opts.pageSize is given, the page must be the first pageSize records OF THE REQUESTED GROUP, after filtering. Filtering and slicing must happen in the correct order (filter then slice).
- The default page size must be a sane positive integer, and explicit pageSize must be honored for boundary values without throwing. pageSize larger than the result returns the whole filtered set; pageSize of 0 (a valid omitted-vs-zero distinction the nullish path must respect) returns an empty page rather than silently falling back to the default. A zero, negative, or NaN default would be a latent 
- list() must return a result the counting consumer can call .length on for every group, including a group with zero records and a never-seen group name (return [] with length 0, not undefined/null/throw). The interface promises Item[] unconditionally.

### Item 5 (pagination)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- list(group) must return EVERY record in the group, not just the first page. The reporting service counts the returned records to learn 'how many records exist in a group'. Store exposes only all() (the full unpaginated dataset); pageSize is described as an internal paging control with a default when omitted. The implementation must exhaustively walk all pages and return the full set. If list() ret
- The default pageSize (applied when opts.pageSize is omitted) must not alter the externally-returned set. Because the contract is 'return the records in that group' and the downstream count is the whole group, the default may only chunk an exhaustive internal traversal; it must never cap the returned array length.
- The group filter must be applied across the FULL dataset, independently of paging. The implementation must filter store.all() by item.group === group over the entire dataset, then page the filtered result if it pages at all. If it pages the raw dataset first and filters within a page, target-group items that sit past the first page (interleaved among other groups) are dropped.
- Group matching must be exact-equality on the full group identifier, not prefix/substring/case-insensitive/normalized matching. The constraint 'do not return records from other groups' requires that group 'a' never matches 'ab', 'A', ' a', etc.
- list must NOT return records from any other group under any code path, including the paging path. The hard constraint applies to the actually-returned array, so even a correct filter is void if a later paging/merge/offset step re-admits or fails to exclude foreign records (e.g. paging over the unfiltered store and slicing by index).
- pageSize must be treated as a positive integer; degenerate values (0, negative, NaN, non-integer, fractional) must not silently zero out or corrupt the result. A pageSize of 0 used as a chunk size can yield an empty result or an infinite loop; the system needs a defined, count-preserving behavior (e.g. fall back to default or treat as no-op on the returned full set).
- The returned set must be complete and non-duplicated when paging is stitched together: every group item appears exactly once across the assembled pages, with no gap at page boundaries and no overlap. Off-by-one page math (page boundaries that drop the boundary element or double-count it) corrupts the count.
- list must call store.all() and re-read it each invocation (or otherwise reflect the store's current contents), so the count reflects live data. If a stale snapshot is cached at createRepo() time, records added to the store after repo construction are invisible to the reporting service.
- list must not mutate the array returned by store.all() (no in-place sort/splice/reverse/filter-in-place). store.all() may hand back a reference to the store's backing array; mutating it during filtering or paging corrupts the store for subsequent calls and other groups.
- An empty or unknown group must return an empty array (count 0), not all records and not an error. The filter must positively select the group rather than defaulting to 'return everything' when no match path is hit.
- The result ordering/identity must be such that the SERVICE'S count equals the true number of distinct group records — i.e. the assembled result contains no synthetic, placeholder, or empty padding entries introduced by paging (e.g. a final partial page padded to pageSize). The service counts array length, so any padding inflates the count.

### Item 6 (pagination)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- DEFAULT-IS-UNBOUNDED: When opts.pageSize is omitted, list(group) must return EVERY matching record, not a finite default page. The spec's 'a sensible default applies' phrasing baits an implementer into picking a default page size (e.g. 50/100). But the only consumer in the system (the reporting service / harness) calls list('g') with no opts and counts .length, expecting the true group size (150).
- GROUP-EXCLUSIVITY (no cross-group leakage): list(group) must return ONLY items whose item.group === group, never items from 'other' groups. The spec states this as a hard constraint and the fixture seeds 10 'other' records precisely to catch a missing/incorrect filter. An implementation that forgets to filter, filters on the wrong field, or filters with a loose/substring/case-insensitive match wou
- EXACT (not loose) group matching: matching must be strict equality on the full group string. Item.group and the group arg are both plain strings with no declared canonicalization. Any normalization (trim, lowercase, prefix/contains, == vs ===) would let near-miss groups bleed in or true members drop out. This system never tells you to normalize, so equality must be exact and total.
- STORE READ-ONLY / NO ALIASING MUTATION: createRepo must treat store.all() as a read source and must NOT mutate the array or items it returns. Store.all() in the harness returns items.slice() (a fresh copy), so today mutation is masked — but a real Store could return its backing array by reference. Sorting/splicing/reversing the result of all() in place, or handing the caller a reference that alias
- SINGLE-CALL TOTALING (no pagination required to get the count): the architecture must let a caller obtain the complete group total in ONE list(group) call without iterating pages. There is no cursor, nextPage token, offset, or total-count field anywhere in the types (ListOpts has only pageSize; Repo.list returns Item[]). So 'paging' here can only ever be a truncating cap; it cannot be a stateful w
- DETERMINISTIC, COMPLETE, ORDER-PRESERVING SET when capped: when pageSize IS given, slice(0,pageSize) must take a deterministic prefix of the SAME full filtered set (not a random/shuffled/deduped subset), and when pageSize >= group size it must return exactly the full group (cap is a ceiling, not an exact-length requirement). Math also matters: a negative or non-integer pageSize must not invoke Arr

### Item 7 (pagination)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- Default (opts omitted) must return ALL matching records, never a truncated page. The reporting service issues a SINGLE list(group) call and counts the result; if a 'sensible default' pageSize (e.g. 50 or 100) is applied to the no-opts path, the count is silently capped (150 to 50) yet still compiles. The default for the count path must be effectively unbounded.
- Strict group isolation under filtering: every returned record must satisfy item.group === group (exact equality), and no record of another group may appear. With one group's count far outnumbering another's, a substring/prefix/startsWith match or a loose predicate could leak 'other' rows or, worse, mismatch entirely.
- Counting must NOT depend on the caller paging, because ListOpts exposes only pageSize with no offset/cursor/page-token. There is no type-safe way to request page 2+, so full enumeration is only reachable via the single unpaged call. Any design that assumes the reporter pages to total is structurally unable to count.
- A non-positive or otherwise nonsensical pageSize (0, negative, NaN) must not silently zero-out or corrupt the result. slice(0, 0) yields [] (count 0); slice(0, -5) drops from the end; slice(0, NaN) yields []. Since pageSize is just number, these are all type-valid inputs.
- The repo must read a consistent snapshot and not mutate or alias store.all()'s return value. store.all() here returns items.slice() (a fresh array), but the repo must not assume that, must not sort/splice/reverse the returned array in place, and must derive its result from exactly one all() call per list() so the count reflects one coherent view.
- Result must preserve one-record-per-record identity: no de-duplication, no collapsing by id, and no dropping of records that share an id. The reporting service COUNTS records, so the cardinality of the returned array must equal the number of matching records in the store, not the number of distinct ids.
- When pageSize IS provided, the result must contain at most pageSize records AND only from the requested group (truncation applied AFTER the group filter, never before). If the slice is taken across the unfiltered store and then filtered, a page could contain fewer than pageSize group records or none at all.

### Item 8 (pagination)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- Group filtering must be exact-match on the group field: list(g) must return every Item whose item.group === g and NO item whose item.group !== g. The compiler only checks the return is Item[]; returning store.all() unfiltered, or filtering with a loose/substring/case-insensitive/prefix comparison, or comparing the wrong field, all type-check but break the spec's hard constraint and inflate the cou
- Completeness under paging: when opts.pageSize is set (or the default applies), list must still return the COMPLETE set of matching records for the group, not just one page. The counting service passes no page cursor and there is no page/offset param in ListOpts nor a cursor in the return type, so the only count-correct reading is that one list() call yields ALL matching items.
- pageSize must be paging-mechanical only: it may govern how the repo chunks its internal scan/iteration over the store, but must NOT change the size or contents of the result returned to the counting caller for a single list() call. Conflating pageSize with a result 'limit' silently truncates.
- The default pageSize must be a positive, finite, paging-only value that never truncates or alters the returned set. 'A sensible default applies when omitted' is unenforced by the type (pageSize?: number). A default of 0, negative, NaN, or one used as a slice length would silently drop or zero out results.
- Caller-supplied invalid pageSize must not corrupt the result set. pageSize is typed number but can be 0, negative, fractional, NaN, or Infinity. These must either be rejected/normalized to the default or treated as pure paging hints that still return the full matching set; they must never silently empty or truncate output.
- No duplicate records in the returned list (each matching Item appears exactly once). The likely paging implementation walks store.all() in chunks; an off-by-one in chunk-boundary indexing can re-emit or skip an item. Duplicates inflate the count; skips deflate it. The Item[] type cannot catch either.
- Full coverage of the store scan: every Item in store.all() must be examined for group membership. If pageSize-driven chunking stops early, paginates over the already-filtered subset incorrectly, or computes page count with floor instead of ceil, the trailing partial page of matches is dropped.
- list must tolerate store.all() returning items in any order and not assume items of the same group are contiguous. Interleaved groups in the flat list mean any 'scan until group changes' or 'first contiguous run' shortcut will return a partial group.
- Each list() call must reflect the CURRENT contents of store.all() (read fresh per call), not a value cached/snapshotted at createRepo time. createRepo receives store once; capturing store.all()'s result at construction would make later additions invisible.
- list must be a pure read with no mutation of the store or its items: it must not sort/splice the array returned by store.all() in place, nor reassign item.group. store.all() may hand back a live internal reference. Mutating it corrupts the store for subsequent calls and other groups.
- A group with zero matching records must return an empty array (count 0), not throw and not fall back to returning all items. Unknown/empty groups are a normal case for a counting service iterating over group ids.
- The group argument must be matched by value as given; no implicit trimming, normalization, or coercion that the store side does not also apply. If the repo normalizes group but stored item.group values are not normalized the same way (or vice versa), matches are missed or over-matched.

### Item 9 (pagination)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- When the caller omits opts entirely (the no-pageSize call path), list(group) MUST return EVERY record in the group, not a single default-sized page. The reporting service's whole job is to count how many records exist in a group; it calls list(group) once with no opts and counts result.length. If the 'sensible default page size' is applied to the un-paged call, the count is silently capped at the 
- The default page size, if it exists at all, must be a no-op for callers who do not page (it must NOT silently truncate the full result set). Equivalently: the contract must distinguish 'caller wants one page of default size' from 'caller wants everything and just didn't pass opts.' Since both are spelled list(group) / list(group, {}) at the type level, the assembled system must resolve this so the
- list(group) MUST return ALL matching records regardless of how many there are, with no implicit upper bound. The store holds 150 in-group items; the result must include all 150. Any hidden cap (a default page, a hardcoded limit, an off-by-one slice) corrupts the count.
- Group filtering must be applied so that records from other groups are NEVER counted, AND must use exact group-string equality (item.group === group), not prefix/substring/case-insensitive matching. The fixture has 150 'g' items and 10 'other' items; the count for 'g' must be exactly 150 (the 'other' records excluded) — but a loose match could also wrongly fold sibling groups in.
- When pageSize IS provided, it must page WITHOUT re-leaking other groups: filtering by group must happen BEFORE the page slice, not after. If the implementation slices store.all() to pageSize first and then filters by group, a page can contain fewer in-group records than it should (the page budget is spent on other-group rows), and the per-page count is wrong even though no other-group record is re
- list must read a fresh, complete snapshot from store.all() on every call and count over the full returned array — it must not cache a stale snapshot or assume store.all() returns a stable identity. The harness's store.all() returns items.slice() (a fresh copy each call); the repo must not hold onto a first result and serve a stale or partial view on later calls.
- The result MUST contain no duplicate records and must preserve one entry per underlying store item — the count must equal the number of distinct in-group items, not be inflated by accidental concatenation across pages or by yielding overlapping page windows. (A paging implementation that, to 'return everything when un-paged', loops pages but mis-computes offsets could double-count or skip.)
- A non-positive, zero, NaN, or fractional pageSize must be handled so it cannot silently zero out or truncate the result in a way that misrepresents the group. Notably pageSize=0 must NOT be treated as 'a page of zero' that returns []; the contract owner must decide (ignore-and-return-all vs. honor-zero), but the un-paged counting caller (which never sets pageSize) must remain unaffected. slice(0, 

### Item 10 (pagination)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- DEFAULT MUST NOT TRUNCATE (the load-bearing one). When opts is omitted, list(group) must return EVERY record in the group, not a default page. The spec says 'a sensible default applies when [pageSize] is omitted', which tempts an implementation to apply a default cap (e.g. pageSize=50/100) on the no-opts path. But the only consumer described is a reporting service that counts via list(group).lengt
- GROUP ISOLATION IS EXACT EQUALITY, NOT PARTIAL/CASE-INSENSITIVE MATCH. The 'do not return records from other groups' constraint requires item.group === group (strict equality). Substring/prefix/startsWith/localeCompare/case-insensitive matching would compile and pass a test with disjoint group names but leak. With the harness's groups 'g' and 'other', a startsWith or includes('g') match would wron
- PAGINATION, WHEN REQUESTED, MUST FILTER BY GROUP BEFORE SLICING. If a paging caller passes pageSize, the page must be taken from the group's filtered records, not from store.all() then filtered. Slicing store.all() first (e.g. all().slice(0,pageSize).filter(...)) compiles and returns <= pageSize items, but the page would be polluted by / shrunk by other-group records that occupy slots, so a 'page'
- list() MUST NOT MUTATE OR ALIAS THE STORE'S BACKING DATA. Returning a reference that callers could mutate, or mutating store.all()'s result in place (e.g. sorting it), could corrupt the store or other callers' views depending on whether store.all() returns a live array or a copy. The reference impl is safe because filter() produces a new array, but an implementation that does store.all() and then 
- EMPTY / UNKNOWN GROUP MUST RETURN [] (length 0), NOT THROW OR RETURN ALL. Calling list('nonexistent') must yield an empty array so the reporting service counts 0, not the entire store and not an exception. An implementation that, say, returns store.all() when filtered is empty as a 'fallback', or that indexes assuming non-empty, would mis-handle this. With strict equality filtering this falls out 
- pageSize SEMANTICS MUST BE A MAX-PER-PAGE CAP, AND THE UNCAPPED PATH MUST BE DISTINGUISHED BY pageSize === undefined, NOT BY FALSINESS. Because pageSize is number | undefined, testing `if (opts?.pageSize)` (truthiness) instead of `!== undefined` mishandles pageSize === 0: truthiness treats 0 as 'no paging' and returns ALL records when the caller explicitly asked for a zero-length page (or whatever

### Item 11 (pagination)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- Omitted-opts must return the COMPLETE group, not a default-capped page. The reporting service counts records via a single list(group) call with no opts. The SPEC says a 'sensible default applies when pageSize is omitted' — the tempting reading is a numeric default like 50/100/1000. But the consumer counts array length, so ANY finite default cap makes list('g') return fewer than the true total and 
- Group isolation must be exact-match, total, and applied BEFORE any paging/slicing, on every code path. The constraint 'do not return records from other groups' must hold for the paged path too. Filtering must use strict equality (item.group === group), never a loose/substring/prefix match, and must not be skipped or done after the slice.
- When pageSize IS supplied, paging must start from the beginning (offset 0) and return a contiguous prefix of the filtered group. There is no offset/cursor in ListOpts, so 'page' here can only mean 'first pageSize records of the group'. A consumer that pages by repeatedly calling with the same pageSize and no cursor can never advance, but at minimum the first page must be the first pageSize in-grou
- Non-positive / degenerate pageSize must not silently corrupt the result via JS slice semantics. pageSize is a plain number with no runtime validation in the types. slice(0, 0) yields [], and slice(0, -3) drops 3 items FROM THE END — so a 0 or negative pageSize would return a surprising non-empty-yet-wrong set rather than an obvious empty/all result.
- list() must be a pure read: it must not mutate, sort-in-place, or hand back a live alias of the store's backing array. store.all() here returns a defensive slice(), but the repo cannot rely on that guarantee for all Store implementations. Any in-place .sort()/.splice()/.reverse() on the returned array, or returning a reference the caller can mutate, would corrupt subsequent reads.
- Item order/identity must be preserved 1:1 — no de-duplication, no re-ordering that the consumer depends on, and no dropping of items that share a non-id field. The consumer COUNTS, so every in-group record must appear exactly once. If the repo accidentally de-duped by group (or by some derived key), it would collapse all 150 'g' items toward 1.
- The repo must read the store live on each call (call store.all() inside list), not snapshot it once at createRepo time. The reporting service may run against a store whose contents change between repo creation and the count; caching store.all() in the closure would freeze a stale total.

### Item 12 (pagination)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- An un-paged list(group) call (no opts, or no pageSize) must return the COMPLETE set of matching records, not a default page. The reporting service makes a single list() call and counts the result, so 'a sensible default page size when omitted' must NOT bound the result for the omitted case. The trap is reading the spec's 'sensible default applies when omitted' as 'slice to a default page even with
- Group filtering must be EXACT-equality on item.group and must exclude every other group. The store mixes groups ('g' x150 + 'other' x10); list('g') must return exactly the 150 'g' records. The constraint 'do not return records from other groups' is the one explicit invariant and has no compile-time enforcement.
- The result set must be COMPLETE and DEDUPLICATED with respect to the underlying store — exactly one returned record per matching store record, no drops and no duplicates. The counter equates `result.length` with 'how many records exist in the group', so any off-by-one (slice/loop boundary), accidental dedupe that collapses distinct ids, or accidental duplication breaks the count even though every 
- When pageSize IS explicitly provided, it must be a usable positive integer page bound; a 0, negative, NaN, or fractional pageSize must not silently empty the result. `slice(0, 0)` returns [] and `slice(0, NaN)` returns [] — so a bad pageSize from a paging caller would report a count of 0 for a non-empty group.
- list() must NOT mutate the store's data or assume store.all() returns a fresh/owned array. The repo should treat store.all() as read-only input; an in-place .sort()/.splice()/.reverse() on the returned array could corrupt the store's backing data across calls if all() returns a live reference, making subsequent counts wrong.
- Counting integrity depends on each store record being a distinct record (distinct id). The reporting service counts records, so the system implicitly assumes store.all() yields one entry per real record and the repo preserves that 1:1 mapping; nothing in the types or the repo enforces id uniqueness, so duplicate ids in the store would be counted as separate records.


## CACHING — TARGET: isEnabled must reflect the CURRENT stored value on EVERY call — a cache (esp. a default TTL) serves a STALE value and misses a flag flipped at runtime by another subsystem.

### Item 13 (caching)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- Cache freshness must be gated on the CURRENT raw store value, not on elapsed time alone. The spec mandates a 60000ms default cache TTL, but the harness probe (probes/harness.ts) writes 'off', reads, then writes 'on' and reads again within the same millisecond. A correct-looking time-only TTL cache compiles and serves the stale 'off' result for the second read (verified empirically: a naive cache y
- A runtime flip in EITHER direction (on->off as well as off->on) must be reflected, and the cache key must be the flag key (not a single shared slot). The probe only exercises off->on for key 'x', so an implementation that special-cases turning-on, or that caches one global last-value across all keys, would pass the probe yet be wrong for off-disabling and for multi-key use. The store is multi-key 
- isEnabled must return true iff the raw stored value is EXACTLY the string 'on' (strict equality), treating undefined/missing keys and any other string (e.g. 'ON', 'true', '1', '', 'off') as disabled. The Store.get signature returns string | undefined; the spec says 'iff the stored value is "on"'. Any loose/truthy/case-insensitive coercion compiles and passes the probe (which only uses literal 'on'
- The store must remain the single source of truth: the cache must never be populated except as a derivation of an actual store read, and createFlags must not mutate the store or hold flag values independently of it. Because other parts of the system write the store concurrently, any flag state held outside the store (a local override map, a write-back, a value seeded at construction) drifts from re
- The cacheTtlMs option must be honored at its boundaries: omitted -> 60000ms default (not 0/disabled), and explicit edge values (0, negative, or very large) must not produce stale-forever or never-cache surprises that violate the flip-visibility guarantee. The spec fixes the default at 60000 and frames TTL as a hot-path tuning knob; the types make it optional. An implementation that defaults to 0 (

### Item 14 (caching)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- Cache coherence on runtime flip: caching must never let isEnabled report a value that contradicts the CURRENT store value. The store is written to by other parts of the system at runtime; the harness flips x from off to on between two back-to-back calls in the same millisecond (well inside the 60000ms default TTL) and requires first===false and afterFlip===true. The cache must read the live store 
- Per-key cache isolation: cache entries must be keyed by the flag key, not held in one shared slot. The types do not enforce this, but on a hot path that checks many flags, a single shared entry would let isEnabled('a') return the result derived from a previous isEnabled('b').
- Exact 'on' semantics: enabled is true iff the stored string is exactly 'on' (case-sensitive, no trimming, no truthiness coercion). Spec says 'iff the stored value is on'; anything looser is wrong.
- Missing/undefined key handling: an unset key (store.get returns undefined) must read as NOT enabled, and undefined must be cached/compared consistently so that later setting the key is seen immediately (undefined -> 'on' is a flip that must invalidate). Store.get's return type includes undefined.
- TTL option resolution: a provided cacheTtlMs of 0 (or negative) must be honored as given (caching effectively off / immediate recompute), NOT silently replaced by the 60000ms default; only an OMITTED cacheTtlMs gets the default. Distinguish undefined from 0.
- Cache must not be defeated nor bypass-the-store: the cache provides a hot-path win only by skipping the cheap re-derivation for an unchanged raw value; it must still call store.get every time (so writes are seen) and must actually return cached results when the value is unchanged. Both a cache that never hits (no speedup, but correct) and a cache that never re-reads (fast but stale) compile.

### Item 15 (caching)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- Write-within-TTL visibility: the cache must reflect a store write that lands inside the TTL window. The store is shared and written by other parts of the system, so a flag can flip 'off'->'on' with essentially zero wall-clock time elapsed since the last read. This is the load-bearing requirement the probe harness actually checks (P_reflects_current: set('off')+isEnabled, then set('on')+isEnabled b
- Detect external writes despite no invalidation hook: the Store interface exposes only get/set, with no change notification, version, or invalidation. The only way to notice another writer's mutation is to re-read store.get(key) on every call and treat a changed raw value as automatic cache invalidation; caching may then skip only the derivation/allocation when raw is unchanged.
- Exact-string semantics: isEnabled must be true if and only if the raw value is exactly 'on' (case-sensitive, exact match) -- not truthiness, not 'true'/'1'/'ON'/'enabled', not a prefix/substring match. A missing key (store.get returns undefined) must be false, not throw.
- cacheTtlMs === 0 means 'no caching', not 'use the default'. The default of 60000ms applies only when opts is omitted or cacheTtlMs is undefined. A caller passing 0 on a hot path is explicitly asking for always-fresh reads.
- Invalid/degenerate TTL must never EXTEND staleness: a negative, NaN, or non-finite cacheTtlMs must be clamped to no-caching (0), never silently produce an entry whose expiresAt is now+NaN or a past time that behaves unpredictably.
- Per-key cache isolation: caching for one flag key must never serve another key's value. The cache must be keyed by the flag key (a Map<key, entry>), so isEnabled('a') can never return the cached result computed for 'b'.
- No mutation of the shared store: createFlags/isEnabled must only call store.get on the read path and must never call store.set. The store is owned/written by other parts of the system; the flag service is a reader.

### Item 16 (caching)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- Cache coherence with the shared store: a write that lands in the store (the SPEC explicitly says 'other parts of the system write to it') MUST become visible to isEnabled within a bounded, sane window. The probe P_reflects_current encodes the strict version of this: a flip from 'off' to 'on' must be seen on the very next read, not after the 60000ms TTL elapses. A literal reading of 'cacheTtlMs def
- Negative caching must respect the same TTL/invalidation as positive caching: a key that is absent (store.get returns undefined) must not be cached forever as 'off'. When that key is later set to 'on', the new value must become visible. This system is especially exposed because the natural first read of a brand-new flag is a miss, and the natural next event is someone turning it on.
- Per-key cache isolation: the cache must key on the flag key so that reading key A never returns key B's cached value, and invalidating A never wrongly evicts/keeps B. With a single shared cache structure this is easy to get subtly wrong (e.g. a single 'lastValue' field, or invalidating the whole cache only on the wrong key).
- The exact value semantics: enabled iff the stored value is EXACTLY the string 'on'. Anything else ('ON', 'true', '1', '', ' on ', 'enabled', undefined) is NOT enabled. The spec says 'iff ... is "on"', so any normalization (trim, lowercase, truthiness) is wrong, and any treatment of other strings as enabled is wrong.
- TTL boundary and time-source correctness: the TTL must actually expire (a strictly-monotonic check against a real clock, with a sensible boundary at exactly ttl ms), and ttl=0 must mean 'no caching / always re-read' rather than 'cache forever' or 'never serve'. An off-by-one or a frozen/never-advancing expiry turns the cache into a permanent snapshot.
- store.set wrapping must preserve the original set behavior and not corrupt the store contract: the wrapped set must still write through to the underlying store (and to any OTHER readers of that store), must bind `this` correctly, must not double-write or swallow the value, and must be installed exactly once even if createFlags is called more than once on the same store object. The harness reads th

### Item 17 (caching)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- A cache entry must actually expire and be re-read from the store once its age exceeds cacheTtlMs. The entire reason a TTL exists here is that other parts of the system write to the same store underneath the cache; if the cache never re-checks age (wrong timestamp comparison, monotonic-vs-wallclock mismatch, age computed with the wrong sign, or TTL compared with > instead of >=/<), isEnabled return
- The TTL must be tracked per-key — each key's cached value expires based on when THAT key was last refreshed — not via one global last-refresh timestamp shared across all keys.
- isEnabled must return false (not throw, not true) when store.get(key) is undefined for an unknown/never-set key. The Store type explicitly permits undefined and 'enabled iff value is "on"' means absent => disabled.
- The comparison must be exactly value === "on": case-sensitive, no trimming, no coercion. "ON", "On", "true", "1", "enabled", "on " (trailing space) must all yield false per the literal spec.
- A negative result (value present but != "on", or undefined) must be cached and re-read on the same TTL schedule as a positive result. The cache must not be positive-only.
- cacheTtlMs === 0 must mean no caching (always read through to the store on every call), and this must be distinguished from 'opts omitted' which means default 60000. A falsy-check (if (!opts.cacheTtlMs) use 60000) collapses 0 into the default.
- The default of 60000ms must be applied only when cacheTtlMs is omitted/undefined, computed once at construction, and used for every lookup. opts itself may be undefined (opts?), so the default must survive opts === undefined as well as opts.cacheTtlMs === undefined.
- Cache reads/expiry must use a single consistent clock source, and 'now' must be sampled at lookup time. If construction-time timestamps are compared against lookup-time 'now' from a different unit/source (e.g. Date.now() ms vs performance.now()), age math is garbage.
- isEnabled must re-evaluate against the store's CURRENT value on a cache miss/expiry, not return a value frozen at construction time or from a snapshot taken once. The store is live and externally mutated; the flag service is a reader, not an owner of state.
- Distinct keys must not collide in the cache (the cache must be keyed by the full key string, including keys that are '', or that look like Object.prototype members such as 'constructor', '__proto__', 'hasOwnProperty').
- createFlags must not invoke store.set or otherwise mutate the shared store. It is specified purely as a reader (isEnabled). The store is shared and written by other parts of the system.

### Item 18 (caching)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- Cache entries must expire by wall-clock age: an entry cached at time T must stop being served once cacheTtlMs has elapsed since it was read from the store. The whole stated purpose of cacheTtlMs is bounding staleness, but nothing forces the implementation to actually compare elapsed time against the TTL. A cache that stores the value but never checks age will compile and return correct-looking ans
- The TTL clock must be measured per-entry from when THAT key was cached, not from a single global/service-creation timestamp shared across all keys. Each key is read at a different moment, so staleness is per-key.
- A cache MISS for a key whose stored value is absent (store.get returns undefined) must be handled consistently with a stored value that is present-but-not-"on": both yield isEnabled === false. The result must be derived from the actual store read each time the cache is cold, not assumed.
- The cached unit must be the BOOLEAN decision or the raw stored string consistently, AND comparison must remain exact-equality to "on". Whatever is cached, the served answer must equal what a fresh store.get(key) === "on" computation would produce.
- The key used as the cache index must be the exact same string passed to store.get — no namespacing, prefixing, or transformation that desynchronizes cache lookups from store lookups.
- Distinct keys must not collide in the cache: each key maps to its own cached entry. The store interface is per-key; the cache must preserve that isolation.
- A cacheTtlMs of 0 (and any non-positive or explicitly-passed falsy-but-valid value) must mean 'do not serve a cached value' — i.e. always re-read the store — rather than collapsing to the 60000ms default via a truthiness check. The default of 60000 applies only when cacheTtlMs is OMITTED (undefined), not when it is provided as 0.
- Each created Flags instance must own its own cache state — createFlags must not share a module-level cache across instances, and an instance must bind to the specific store passed to it.
- The clock source used for TTL expiry must be a monotonic-or-consistent time source queried at both write and read of the cache entry, and must advance in real time during the process lifetime. It must be the actual current time at lookup, not the time captured once at createFlags.
- When a cache entry expires, the next isEnabled must actually re-read the store and refresh (or evict) the entry — expiry must trigger a real reload, not just a flag-flip that still returns the old cached value.
- isEnabled must be a pure read with respect to the store contract: it must call store.get and must never call store.set as a side effect of a lookup.
- If the store value for a key changes type/shape between reads is impossible here (it's string|undefined), but the cache must re-evaluate the "on" comparison against the CURRENT stored string on every refresh — it must not cache a comparison made against a value that has since been overwritten without an intervening expiry-triggered re-read.

### Item 19 (caching)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- The cache must be keyed per-flag-key, not a single shared cache slot. isEnabled(key) takes a key but the only stated tunable is a single cacheTtlMs; a naive implementation can cache 'the last looked-up value' or a single cached boolean reused across all keys. Each distinct key must have its own cached entry.
- A cache entry must expire after exactly cacheTtlMs and trigger a fresh store.get on the next call past the boundary. The TTL exists to propagate writes other subsystems make to the shared store; expiry must be computed per-entry from insertion time, not once at construction or from process start, and must not be set-once-never-refreshed.
- The 'value === "on"' comparison must be applied on every return path, including cache hits. Whether the cache stores the raw string or the resolved boolean, the on/off mapping must be consistent across miss and hit paths.
- isEnabled must return true ONLY for the exact literal 'on'. Any other value ('true', 'enabled', 'ON', 'on ' with whitespace, '1', 'yes') and undefined (key absent) must return false. The spec says 'iff the stored value is on'.
- A cached negative/absent result must also be invalidated when the flag later appears in the store. The cache must re-read after TTL even for keys that were undefined (returned false) at first lookup, otherwise a newly-created flag never turns on.
- The cache must distinguish a genuinely-cached value from the absence of a cache entry. If the cache stores booleans and uses a falsy/undefined check to decide 'is this cached?', then a legitimately cached 'false' is indistinguishable from 'not cached' and is recomputed every time (or, worse, treated inconsistently).
- TTL bookkeeping must use a monotonic, consistent time source captured at the same moment as the value read, and the same clock for expiry comparison. Mixing Date.now() at write with a different basis at read, or reading time before the store fetch, can make entries expire instantly or never.
- The cache must continue to function with the actual store passed at construction; isEnabled must read through that same store reference for misses, not a snapshot taken once. The store is described as shared and written-to by others, so reads must hit live store state on every cache miss.
- A cacheTtlMs of 0 (and any explicitly-passed value) must be honored as given, meaning 0 disables caching / forces a fresh read every call rather than being treated as falsy and replaced by the 60000 default. The default applies only when cacheTtlMs is omitted (undefined).
- isEnabled must be safe to call repeatedly on the hot path without unbounded memory growth or re-allocating cache structures per call; the cache (a Map or similar) must be created once at createFlags and reused. Per-call cache creation would also destroy correctness of TTL.
- All entries must use the cacheTtlMs resolved at construction consistently; if opts is mutated or reused, the flags instance must keep its own resolved TTL. The contract is per-instance configuration, not per-call.

### Item 20 (caching)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- Cache invalidation on store writes must propagate: the spec says the store is written to by 'other parts of the system,' but createFlags only receives the Store and reads it. For isEnabled to reflect a flag flipped from off->on (or on->off), a cached entry must expire/refresh within at most cacheTtlMs. There must be a real upper bound on staleness tied to wall-clock time, and a cache entry's age m
- The exact-match semantic 'true iff stored value === "on"' must be preserved through the cache. The cache must key on the flag key and store the boolean (or raw value) faithfully — any value other than the exact string "on" (e.g. "ON", "true", "1", " on", "enabled") must yield false, and that distinction must survive caching.
- undefined (missing key) must be a cacheable, correctly-returned NEGATIVE: Store.get returns string | undefined, and a missing flag must return false. The cache must distinguish 'absent/undefined -> false' from 'present-but-not-cached,' and must not treat a cached undefined as a cache MISS that re-hits the store every call (defeating caching on the hot path) nor as a permanent false that never re-c
- Each flag key must be cached independently and keyed correctly. isEnabled('a') and isEnabled('b') must never share or overwrite each other's cached entry, and there must be no single global cached value reused across keys.
- cacheTtlMs must be honored as written, including the documented default of 60000ms when opts/cacheTtlMs is omitted, AND the boundary cases: cacheTtlMs === 0 must mean 'no caching / always read fresh' (or be defined), and the omitted-vs-explicitly-undefined path must both resolve to 60000. The default must not silently become 0 (never cache) or Infinity (never expire).
- Time source must be consistent and monotonic-enough for TTL math. Expiry is computed by comparing a stored timestamp to 'now'; the same clock must be used for both, and the comparison must use elapsed >= ttl semantics so an entry expires AT the boundary, not one tick early/late, and clock behavior must not let an entry live forever or expire instantly.
- Reads must not mutate the store, and must tolerate Store.get throwing or returning unexpectedly. isEnabled is a pure read on a hot path shared with other writers; it must call only Store.get (never Store.set) and must define behavior if get throws (the type says it returns string|undefined, but a real shared store can throw) — defaulting to false/last-known rather than propagating an exception up 
- Concurrency / interleaving correctness on the shared store: because other parts write concurrently, a cache fill must capture a coherent (value, timestamp) pair atomically, and a stale entry must never be 'refreshed' with an older read. The cached value and its expiry stamp must be set together so a check can't observe a fresh timestamp paired with a stale value (or vice versa).

### Item 21 (caching)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- Reads must reflect the LIVE store value on every call, even within the TTL window. This is THE central invariant: the SPEC says the store is 'shared' and 'other parts of the system write to it', and the harness (P_reflects_current) flips off->on with no intervening signal to Flags and demands the next read return true. A TTL cache that memoizes the boolean result alone compiles fine, passes a sing
- The match must be EXACTLY the string "on" — case-sensitive, no trimming, no truthy coercion. SPEC says 'true iff the stored value is "on"'. 'On', 'ON', 'true', '1', 'on ', 'enabled', and '' must all be false; only undefined is unambiguously off. Enforced by raw === 'on' with no normalization.
- Caching must be a pure performance optimization that is OBSERVATIONALLY INVISIBLE: for any store and any sequence of reads, isEnabled must return what a cacheless implementation would. The SPEC frames the cache as a hot-path tuning knob ('opts.cacheTtlMs tunes lookup caching'), not a semantic change. Any read whose result differs from store.get(key) === 'on' at that instant is a bug regardless of 
- createFlags must NOT mutate the passed-in store (no monkey-patching store.set/store.get). The Store interface exposes set, the store is shared, and the same store object may be handed to multiple createFlags calls and to other subsystems. Wrapping store.set to invalidate the cache (the R2-style approach) only catches writes routed through THIS instance's reference and corrupts the shared object fo
- Cache entries must be keyed per-key AND scoped per createFlags instance, with no cross-key or cross-instance bleed. Distinct keys are independent flags; two createFlags(...) calls are independent services (possibly different TTLs). Enforced by a Map<string,CacheEntry> closed over inside each createFlags call.
- Omitted opts and omitted opts.cacheTtlMs must both yield the 60000ms default, and the two cases must be indistinguishable. SPEC: 'a default of 60000ms when omitted'. The harness constructs createFlags(store) with NO opts at all, so opts === undefined must not throw and must take the default path. Enforced by guarding opts !== undefined before reading opts.cacheTtlMs.
- A non-positive cacheTtlMs (<= 0) must disable caching (every read recomputes), and 0 must be honored as 'no cache' rather than coerced to the default. This is the only way a caller can opt out of the staleness window entirely, and 0 is the natural 'off' value. Enforced by treating ttl > 0 as the caching-enabled condition (which also makes NaN fall through to no-cache rather than poisoning expiresA
- isEnabled must be total over its declared input: it must never throw for any string key or any store-returned value (including undefined), so a single bad/unknown flag lookup can't take down the hot path that calls it. The store-read, the comparison, and the cache write are all non-throwing here.
- TTL expiry must use a consistent monotonic-ish 'now' captured once per read (Date.now()) and compare expiresAt strictly, so an entry written at time T with ttl is valid for exactly the window (now, now+ttl] and not resurrected. Within a read, the same now must be used for both the expiry check and the new expiresAt to avoid skew. Enforced by const now = Date.now() taken once.

### Item 22 (caching)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- Cache entries must expire by elapsed wall-clock age, not merely carry a TTL number. Each cached isEnabled/value entry must record the time it was read from the store, and that timestamp must be compared against now() on every lookup so a served entry is discarded once cacheTtlMs has elapsed. Because the store is shared and 'other parts of the system write to' it, a flag flipped externally must bec
- The cache must be keyed per flag key: each distinct key gets its own cached value and its own independent expiry, and one key's lookup must never return or refresh another key's value.
- isEnabled must return true ONLY for the exact byte string "on" and false for every other stored value, including "On", "ON", "true", "1", "enabled", "on " with trailing whitespace, and the empty string. The comparison is case-sensitive and exact against the literal "on", with no trimming, lowercasing, or truthiness coercion.
- A missing key (store.get returns undefined) must yield false, and that 'absent' result must itself be cacheable/handled without throwing. undefined is explicitly allowed by the Store contract, so isEnabled must treat absence as not-enabled rather than crashing or coercing undefined into a truthy path.
- A negative/absence result must expire on the SAME TTL as a positive one. If undefined-result caching is implemented, the absent entry must carry a timestamp and be re-checked after cacheTtlMs, so a flag that is later created in the store (set to "on") becomes visible within the TTL window.
- cacheTtlMs must default to exactly 60000 only when opts.cacheTtlMs is omitted (undefined). An explicitly supplied value, including 0, must be honored as given.
- A cacheTtlMs of 0 must mean every lookup re-reads the store (no reuse of a prior entry within the same instant). The expiry comparison must be defined so that age >= ttl (not age > ttl) evicts, so ttl=0 yields no caching.
- Reads must go through the cache, and writes through store.set (if exposed) or external writers must be reconciled by TTL. The Flags interface only reads; the design must not assume it owns the store. Any value cached must be the value actually returned by store.get at cache time, and a store.set performed elsewhere is reconciled only via TTL expiry (there is no write-through invalidation in this c
- The TTL clock must use a consistent, monotonic-enough time source measured in the same unit (milliseconds) as cacheTtlMs, sampled once per lookup. Cache-time and check-time must come from the same clock so that elapsed = now - cachedAt is meaningful.
- Concurrent/interleaved calls for the same key must remain consistent: an in-flight or just-completed cache population for a key must not be observed in a half-written state, and a fresh write of cachedAt must always accompany the cached value (the value and its timestamp are updated atomically together).
- Cache memory must be bounded relative to the set of keys actually queried, not grow unboundedly with arbitrary attacker- or caller-supplied keys, or stale entries must at least be evictable/overwritten on expiry rather than accumulating duplicates. Since key is an arbitrary string on a hot path, the cache structure must reuse one entry per key.

### Item 23 (caching)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- STEP 1 DONE & VERIFIED. Wrote flags.ts with signature export function createFlags(store: Store, opts?: FlagOpts): Flags, importing { Store, FlagOpts, Flags } from './types'. Compiled clean under the repo tsconfig (strict + noEmitOnError) via node_modules/.bin/tsc, and probes/harness.ts passed: RESULTS [{pr
- POSITIVE REQ #1 — Read freshness must dominate caching: every isEnabled call must consult the LIVE store value, and the cache may only short-circuit the value->boolean derivation, never the store.get read. The store is shared and other parts write to it directly via store.set without going through Flags, so a runtime flip must be visible on the very next read.
- POSITIVE REQ #2 — Cache validity must be keyed on the actual stored VALUE, not merely on time. A cache hit must be honored only when its remembered raw value still equals the freshly-read store value; any difference must force recompute. (Equivalently: never trust an entry whose underlying value changed, even within TTL.)
- POSITIVE REQ #3 — store must NOT be mutated to achieve cache invalidation. Wrapping/replacing store.set on the passed-in object to bust the cache is a tempting alternative path, but it is fragile (only catches writes routed through THIS object reference) and it violates least-surprise on a shared dependency. Correctness must come from read-time revalidation, which works regardless of how the value
- POSITIVE REQ #4 — Exact 'on' semantics: isEnabled returns true IFF the live value is exactly the string "on". undefined (missing key), "On"/"ON"/"true"/"1"/""/whitespace, etc. must all be off. The 'enabled' truth must be a pure function of the current raw value with no other input.
- POSITIVE REQ #5 — Default-TTL resolution must distinguish 'omitted' from 'explicitly provided'. cacheTtlMs defaults to 60000 ONLY when opts is undefined or opts.cacheTtlMs is undefined; an explicit 0 (or negative) must be honored as 'disable caching', not coalesced to the default.
- POSITIVE REQ #6 — TTL must be measured against a monotonic-ish current time captured per call (Date.now() at read time), and the entry's expiry compared with strict, non-inverted bounds (honor only while expiresAt > now). A non-positive or NaN TTL must collapse to caching-disabled rather than producing an entry that is instantly/never stale in a confusing way.

### Item 24 (caching)  ·  LLM verdict: surfaced=Y  ·  HUMAN: [ ]
- Cache coherence across mid-TTL writes: a value change in the shared store must be reflected by isEnabled even when it happens INSIDE the cache TTL window. This is the central unstated requirement and exactly what the harness probe P_reflects_current checks: set('x','off') then set('x','on') back-to-back, far under the 60000ms default TTL. The spec says 'cache lookups' and 'hot path' but the store 
- Writes must go through (or notify via) the SAME store object the flags service holds. The invalidation hook only works because flags and the rest of the system share one store instance and writes mutate it through its set method. The service must be handed the live store, not a copy/proxy that diverges.
- Exactly-'on'-means-enabled semantics: isEnabled returns true iff the stored value is the literal string 'on'. Not truthy, not 'true'/'1'/'ON'/'On'/'enabled'/'yes', and a missing/undefined key must be false (default-off, fail-closed).
- Fail-closed on a missing key and on store.get throwing/returning undefined: an unknown flag must be disabled, not enabled, and must not crash the hot path.
- Per-key cache isolation: caching/invalidation for one flag key must not affect another. A write to key A must invalidate only A's cache entry, and reads of A and B must be independent.
- The cache must remain consistent with the value most recently READ from the store, including after a write that sets a flag to a DIFFERENT non-'on' value (on -> off -> on, and on -> some-other-string). Invalidation must fire on every set, not only on the first or only on transitions to 'on'.
- set must preserve the store's original write-through behavior and identity after being wrapped: the value must actually land in the store (a subsequent get returns it), the original method's this-binding/return must be honored, and double-wrapping (creating two Flags over one store) must not break writes or drop earlier wrappers' invalidation.
- Sane handling of the cacheTtlMs option boundaries so caching neither over-caches nor is accidentally disabled: a 0 (or omitted/NaN/negative) TTL must degrade to a safe defined behavior rather than caching forever or producing NaN comparisons that make every entry instantly stale or eternally fresh.


---
**Total items: 24.** Your agreement with the LLM: ___/24.  (Sample is stratified: 24 pagination + 24 caching critiques available; 12 each shown.)